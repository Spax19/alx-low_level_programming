#include "main.h"
#include <stdio.h>

/**
 * _mul -  a function that multiples two numbers
 * @c: input
 * Return: result of the two numbers
 */
int numbers(int a, int b);

int mul() {
    int x = 5;
    int y = 7;
    int z = numbers((x, y);
    printf("%d * %d = %d\n", x, y, z);
    return 0;
}
